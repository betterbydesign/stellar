---
description: Update Stellar's durable documentation trail after meaningful work
---

Read `AGENTS.md`, `WORKFLOW.md`, `docs/agent-rules.md`, and the relevant sections of `harness.json`. Update `documentation.artifacts.statusDoc`, the active ExecPlan, architecture docs, user guides, and operations log as the completed work requires. Verify claims against shipped behavior and run applicable `verify.*` commands. Report actual results and remaining work. This command does not authorize commits, pushes, PRs, merges, deployments, or external writes.

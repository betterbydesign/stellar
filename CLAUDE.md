# Claude Code Instructions

This repo's canonical agent instructions live in `AGENTS.md` — one file, every host.
Claude Code inlines it through the import below. Do not add content here: a rule that
belongs to this repo goes in `AGENTS.md` or in the shared harness documents it points
to, so every agent host reads the same policy.

@AGENTS.md

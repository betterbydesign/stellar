# Review Artifacts

Raw review threads and approvals belong on the code host. Keep recurring accepted lessons here only when `documentation.artifacts.reviewLessons` is configured in `../../harness.json`; it is null today. `review` is also null, so this repo has no registered automated reviewer or custom check-run agents. For present review practice, follow `../agent-rules.md` and `../../WORKFLOW.md`.

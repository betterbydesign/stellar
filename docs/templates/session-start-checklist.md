# Session Start Checklist

- Request and acceptance criteria pointer:
- Branch or worktree:
- Scope and assumptions:
- Unrelated work to preserve:
- Applicable `verify.*` groups:
- ExecPlan needed for multi-session, cross-subsystem, risky, or ambiguous work:
- Next action:

Read `documentation.intakeSet` in `harness.json`. Check the worktree before editing. Use an external task ID only when a real tracker is configured or the user supplies one.

# Current Work

Compact handoff pointer. No external task IDs are registered while `tracker` is null in `../harness.json`.

## Active Work

[First-use website experience](exec-plans/first-use-website.md), branch `codex/first-use-website` in worktree `933d`: [PRD](prds/first-use-website.md) and first implementation slice add automatic personal setup, prerequisite-first account UI, durable local creation recovery and timing measurements. Full signed-in source provisioning remains unfinished pending authenticated computer pairing. See [handoff](handoffs/first-use-website.md) and [evidence](evidence/first-use-website/verification.md). Main and the saved checkout are unchanged by this task.


The user authorized consolidation, local synchronization and push on 2026-09-24. Integration is complete on `codex/integrated-foundation`; see the [integration ExecPlan](exec-plans/integrated-foundation.md). It combines reviewed blueprint projects, [selective editor source-preservation fixes](handoffs/stacki-upstream-first-batch.md), [platform foundation](handoffs/e01-platform-foundation.md), [proposal review](handoffs/e01-proposal-review-foundation.md), and the saved checkout's runner lifecycle improvements. Fresh review and combined verification passed; see [integration evidence](evidence/integrated-foundation/verification.md). The saved checkout is synchronized with the verified integration, including its original lifecycle changes now committed. Delivery uses this integration branch; main is unchanged.

Stellar development configuration is now present in the saved checkout. On 2026-09-24 the first Convex deployment to `kindly-otter-616` completed after shortening a rejected index name and replacing offline helpers with real generated bindings. The deployed viewer function is reachable and rejects unauthenticated calls; signed-in persistence and isolation acceptance remain open. Hosted projects stay disconnected until authenticated runner pairing exists. Proposal approval records intent only; provider execution, source application and spending remain disabled. The [platform prerequisite checklist](handoffs/e01-platform-foundation.md) remains open. Prior isolated test results do not prove combined or live-provider acceptance.

Company M2 remains independent: next is [WordPress model and ownership](prds/m2-02-wordpress-model-and-ownership.md), with the [M2 dispatch index](prds/m2.md) recording dependencies. Live schema/import/publication/deployment is unperformed. A watcher backlog reproduced under sustained slow refresh remains a separate runner follow-up, not a platform prerequisite.

## Latest completed work

[M2-01](handoffs/m2-01-review.md), on `codex/m2-content-contracts`: portable offline content contracts/planner, synthetic fixture, command-line review and a blocked company evidence audit. Three SOL high subagents contributed package implementation, source audit, PRDs and independent review; the orchestrator integrated and reviewed the result. Full root verification passed 103 tests plus docs/harness/lint/typechecks/fixture checks; production packages/Next/Astro builds passed. See the [completed plan](exec-plans/completed/m2-content-contracts.md), [decision](decisions/005-content-plan-and-company-poc.md), [architecture](architecture/content-import-planning.md) and [operator guide](user-guide/content-planning.md). M2-02–06 are proposed, not implemented.

The company audit confirms 71 source references across 16 tables/176 fields and keeps the research format non-executable. Actual ACF keys/schema, taxonomy, record values, media evidence and target inventory are missing. No company checkout, live data, remote integration, push, merge or deployment changed.

[M1](handoffs/m1-editor-review.md) remains the usable local Projects/Studio foundation. Start from the repository root with `npm run dev:local` and use the launcher's connection link. Source-linked selection, responsive widths, supported CSS/token edits, reset and durable undo/redo work on registered Astro fixtures. The local launcher uses `.next-local`; web-only development and production use `.next`. M1 user visual review is separate from this CLI-only M2 slice. General CMS/IA, hosted accounts, agents, HTML and deployment UI remain follow-on product work.
